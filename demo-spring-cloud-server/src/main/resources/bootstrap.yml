server:
 port: 8111

spring:
  application:
    name: demo-spring-cloud-consul

---

spring:
  profiles: default
  cloud:
    consul:
      config:
        data-key: data
        prefix: config
#        default-context: apps
#        profile-separator: ','
        format: yaml


